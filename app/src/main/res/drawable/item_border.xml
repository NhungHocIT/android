<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- Màu nền của phần tử -->
    <stroke
        android:width="3dp"
        android:color="@android:color/black" /> <!-- Đường viền -->
    <corners android:radius="4dp"/> <!-- Bo góc -->
</shape>